  npm ci                 # instead of make install-ui
  go install tool        # instead of make install
  npm run build          # instead of make ui
  go run ./...           # run without building a binary